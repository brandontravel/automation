# Ops

Operational notes and safe examples.

- `env.example` shows expected environment variables (non-secret).
- `secrets.example.md` documents what secrets may be needed (if any).

Do not commit real credentials.
