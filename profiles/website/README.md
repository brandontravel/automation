# Website Profile

Use when the org maintains a static website (GitHub Pages or similar).

Includes:
- generic Pages deploy workflow (looks for _site/site/dist/public/docs)
- scheduled link check hook (optional)
- checks scripts placeholders

Outputs:
- deployed site (if configured)
