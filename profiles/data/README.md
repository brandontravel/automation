# Data Profile

Use when the org publishes datasets or structured resources.

Includes:
- basic validation scripts
- packaging script placeholder for release bundles
- optional metadata templates (Zenodo + CITATION)

Outputs:
- `data/dist/` (packaged bundle)
